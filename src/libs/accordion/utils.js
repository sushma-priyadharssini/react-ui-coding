export const getAccordionControlId = (accordionId, value) => {
    return accordionId + '-header-' + value;
}

export const getAccordionRegionId = (accordionId, value) => {
    return accordionId + '-panel-' + value;
}